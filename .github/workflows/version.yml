name: Version

on:
  push:
    branches:
      - main
      - 'pre-release/*'

permissions:
  contents: write
  pull-requests: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - run: pnpm install --frozen-lockfile

      # Guard: pre-release mode must not leak to main
      - name: Check for pre-release mode
        if: github.ref == 'refs/heads/main'
        run: |
          if [ -f .changeset/pre.json ]; then
            echo "::error::pre.json detected on main branch. Run 'pnpm pub:exit-pre' before merging."
            exit 1
          fi

      - name: Create Version PR
        uses: changesets/action@v1
        with:
          version: pnpm changeset version
          title: >-
            ${{ startsWith(github.ref, 'refs/heads/pre-release/')
                && 'chore: version packages (pre-release)'
                || 'chore: version packages' }}
          commit: >-
            ${{ startsWith(github.ref, 'refs/heads/pre-release/')
                && 'chore: version packages (pre-release)'
                || 'chore: version packages' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
